#include<bits/stdc++.h>
using namespace std;


void solve(int n,vector<vector<int>> &a,vector<vector<int>> &v,int x,int y,string path)
{
    int srcx=x;
    int srcy=y;
    
}


int main()
{
    int n;
    cin>>n;
    string path="";
    vector<vector<int> > a;
    for(int i=0;i<n;i++)
    {
        vector<int> temp;
        for(int j=0;j<n;j++)
        {
            int c;
            cin>>c;
            temp.push_back(c);
        }
        a.push_back(temp);
    }
    vector<vector<int> > visisted;
    for(int i=0;i<n;i++)
    {
        for(int j=0;j<n;j++)
        {
            visisted[i][j]=0;
        }
    }
    int x=0;
    int y=0;
    
    solve(int n,vector<vector<int>> a,vector<vector<int>> visisted,int x,int y,string path);
    
    
}
